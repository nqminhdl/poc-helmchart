# ---------------------------------------------
certManager:
  enabled: true
  installCRDs: true
  fullnameOverride: cert-manager
  prometheus:
    enabled: true
    servicemonitor:
      enabled: true
      labels:
        release: prometheus
