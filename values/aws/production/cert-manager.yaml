# ---------------------------------------------
certManager:
  enabled: true

cert-manager:
  installCRDs: true
  fullnameOverride: cert-manager
  prometheus:
    enabled: true
    servicemonitor:
      enabled: true
      labels:
        release: prometheus
